# フロントエンドの環境変数テンプレート。`cp .env.example .env.local` して使う。
# ⚠️ VITE_ プレフィックスの値はビルド成果物に埋め込まれ、誰でも閲覧できる。
#    APIシークレット等の秘密情報は絶対に置かないこと(秘密はバックエンド側で扱う)。

# APIのベースURL(未設定時は同一オリジンの /api)
VITE_API_BASE_URL=

# 開発時のMSWモックを無効化して実APIに接続する場合は false を設定
VITE_API_MOCK=
